# Pinned exact versions for reproducible packaged builds.
# Generated from working dev environment (Anaconda 3.10, Windows, 2026-05-06).
# torch/torchaudio: the runtime bootstrap detects nvidia-smi at install time
# and pip-installs the matching CUDA 12.4 wheel automatically when an NVIDIA
# GPU is present (Windows/Linux x64). Otherwise the CPU wheel is installed.
# Apple Silicon uses the CPU wheel which transparently uses MPS.

# ── Core pipeline ──────────────────────────────────────────────────────────────
# torch/torchaudio are still required (STRUM's CRNN onset models, drums
# engine, vocals charter all import torch directly).
#
# CPU hosts:  stem separation uses a native demucs.cpp binary
#             (src/main/strumIntegration/demucsCppBootstrap.ts).
# CUDA hosts: stem separation falls back to the Python `demucs` package so
#             it can run on the GPU (~10× faster than demucs.cpp on CPU).
# Lyric transcription uses a native whisper.cpp binary
# (src/main/strumIntegration/whisperCppBootstrap.ts) on both, so the
# Python `openai-whisper` package + tiktoken are not needed.
torch==2.6.0
torchaudio==2.6.0
# Python demucs is only invoked on CUDA hosts; demucs.cpp handles CPU.
# Pinned to the last release that targets torch 2.x.
demucs==4.0.1
# demucs's hard deps not already covered above.
julius==0.2.7
lameenc==1.7.0
dora-search==0.1.13
openunmix==1.3.0
diffq==0.2.4
librosa==0.11.0
mido==1.3.3
# basic-pitch auto-selects a backend at import time. We bundle onnxruntime
# below so it falls back to the ONNX model shipped inside the basic-pitch
# package — no TensorFlow required. (basic-pitch's own __init__ logs a
# benign "Tensorflow is not installed" warning we suppress in callers.)
basic-pitch==0.4.0
huggingface_hub==0.36.2
numpy==1.26.4
soundfile==0.13.1
omegaconf==2.3.0
PyYAML==6.0.1
requests==2.33.1
yt-dlp==2026.3.17
pyphen==0.17.2
# onnxruntime variant is selected by the runtime bootstrap:
#   * CPU hosts get `onnxruntime==1.20.0`
#   * CUDA hosts get `onnxruntime-gpu==1.20.0` so basic-pitch can use the
#     CUDAExecutionProvider for MIDI prediction.
# Either way the import name is `onnxruntime`; basic-pitch picks providers
# automatically.
onnxruntime==1.20.0
protobuf==4.25.3

# ── Pinned indirect dependencies ───────────────────────────────────────────────
scipy==1.15.3
numba==0.65.1
llvmlite==0.47.0
resampy==0.4.2
audioread==3.1.0
soxr==1.1.0
lazy-loader==0.5
joblib==1.5.3
decorator==5.2.1
pooch==1.9.0
msgpack==1.1.2
einops==0.8.2
antlr4-python3-runtime==4.9.3
tqdm==4.66.5
click==8.1.7
rich==13.7.1
mir_eval==0.8.2
pretty_midi==0.2.11
cffi==2.0.0
six==1.17.0
packaging==26.2
filelock==3.25.2
fsspec==2026.2.0
typing_extensions==4.15.0
certifi==2026.4.22
charset-normalizer==3.4.7
idna==3.13
urllib3==2.6.3
